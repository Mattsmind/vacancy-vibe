    <nav class="navbar navbar-expand-lg navbar-dark bg-black sticky-top" style="z-index: 1050;">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">VacancyVibe</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="/motels">Motels</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="/motels/new">New Motel</a>
                    </li>           
                </ul>

                <!-- Right aligined login form -->
                <div class="d-flex align-items-center gap-2 ms-auto mt-2 mt-lg-0">
                <% if (currentUser) { %>
                    <span class="navbar-text text-white me-2">
                        Logged in as: <strong><%= currentUser.username %></strong>
                    </span>
                    <form action="/auth/logout" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-outline-danger btn-sm">Logout</button>
                    </form>
                <% } else { %>
                    <a href="/auth/login" class="btn btn-link text-light text-decoration-none btn-sm">Login</a>
                    <a href="/auth/register" class="btn btn-primary btn-sm">Sign Up</a>
                <% } %>
                </div>
            </div>
        </div>
    </nav>
